// EclipseMD3 :: ApplicationWindow — MD3 default font + background
import QtQuick
import QtQuick.Templates as T

T.ApplicationWindow {
    font.pixelSize: 13
    background: Rectangle { color: ThemeBridge.background }
}
